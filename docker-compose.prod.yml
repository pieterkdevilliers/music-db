services:
  backend:
    build:
      context: ./backend
      target: production
    volumes:
      - backend_db:/app/data
    ports:
      - "8003:8003"
    environment:
      DATABASE_URL: sqlite+aiosqlite:////app/data/daily_tasks.db
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      target: production
    ports:
      - "3003:3003"
    environment:
      NUXT_PUBLIC_API_BASE: http://localhost:8003
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  backend_db:
